# Example environment variables for running this compose stack
# Copy to .env and fill in secrets. Do NOT commit real secrets into git.

# SonarQube database settings (example values already set in docker-compose.yml)
# POSTGRES_USER=sonar
# POSTGRES_PASSWORD=sonar
# POSTGRES_DB=sonar

# SonarQube server URL used by mcp-server when calling the API
SONARQUBE_URL=http://sonarqube:9000

# SONARQUBE_TOKEN: required by the MCP server to authenticate to SonarQube's API.
# How to create a token:
# 1. Login to SonarQube as an admin (or a user with permission to generate tokens)
# 2. Go to "My Account" -> "Security"
# 3. Under "Tokens" create a new token, give it a descriptive name, and copy it.
# 4. Add the token below in your local .env file (DO NOT COMMIT):
#
# SONARQUBE_TOKEN=your-generated-token-here

# Optional: MCP server config
# MCP_LOG_LEVEL=INFO
# MCP_OTHER_OPTION=value
